{
  "name": "zipit-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "ZipIt — Client-side ZIP streaming & batch download library",
  "scripts": {
    "dev": "yarn --cwd apps/web run dev",
    "dev:docs": "pnpm --filter @blueneon/zipit-docs dev",
    "build": "pnpm -r --filter '@blueneon/zip-it' --filter '@blueneon/zipit-react' build",
    "build:all": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "release": "pnpm -r --filter '@blueneon/zip-it' --filter '@blueneon/zipit-react' publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.5",
    "@changesets/cli": "^2.27.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
